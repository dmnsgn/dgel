const PI = /* glsl */ `const float PI = 3.14159265359;`;
const EPSILON = /* glsl */ `const float EPSILON = 0.00000000001;`;
const GAMMA = /* glsl */ `const float GAMMA = 2.2;`;

export default { PI, EPSILON, GAMMA };
